(function () {
  "use strict";

  // NEO Stream deliberately ships without invented titles. Movie and TV rows
  // are filled at runtime by the public discovery providers in app.js.
  window.NEO_STREAM_CATALOG = [];
})();
